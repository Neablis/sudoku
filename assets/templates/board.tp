<div id="sudoku_board">
    <% 
        //Defining each cube
        for (var x = 0; x < 9; x++) {
            %>
            <div class="parentCube">
            <%
            //Defining each cell in cube
            for (var y = 0; y < 9; y++) {
                %>
                    <div class="childCube"><%= y %></div>
                <%
            }
            %>
            </div>
            <%
        }
    %>
</div>